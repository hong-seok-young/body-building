# 백엔드(Apps Script) 확인 필요 — 새 필드

## 무엇이 바뀌었나

식단 기록 한 건이 이제 아래 필드를 가집니다. 굵은 것이 새로 생긴 필드입니다.

| 필드 | 뜻 | 비고 |
|---|---|---|
| `date` | 날짜 | 기존 |
| `name` | 음식명 | 기존 |
| `kcal` `p` `c` `f` | 칼로리·단백질·탄수·지방 | 기존 |
| **`meal`** | 끼니 — `아침` `점심` `저녁` `간식` | 신규 |
| **`na`** | 나트륨 (mg) | 신규 |
| **`fib`** | 식이섬유 (g) | 신규 |
| **`sug`** | 당 (g) | 신규 |

목표(`setMeta` 의 `goals`)에도 `na`(나트륨 상한, 기본 2000)와 `fib`(식이섬유 하한,
기본 30)가 추가되었습니다. 계획(`plan`)은 통째로 JSON 한 덩어리로 저장되므로
시트 컬럼과 무관하게 그대로 동작합니다.

## 지금 상태

`Code.gs` 가 시트에 고정된 컬럼만 쓰고 있으면 위 신규 필드는 **서버에 저장되지
않습니다.** 클라이언트는 이 상황에서도 화면이 깨지지 않게 만들어 두었습니다.

- 기록 추가 시 서버 응답에 신규 필드가 없으면 로컬 값을 유지합니다
  (`pushItem` 의 `{ ...item, ...r.item }`).
- 그래서 **추가한 그 세션 동안은** 끼니 구분과 나트륨이 정상으로 보입니다.
- 하지만 앱을 다시 열어 서버에서 다시 받아오면(`syncRange`) 신규 필드가 사라지고,
  `meal` 이 없는 기록은 모두 `간식/기타` 로 들어갑니다.
- 합산은 없는 필드를 0 으로 처리하므로(`totals`) 나트륨·식이섬유가 과소 집계됩니다.

즉 **끼니 구분과 나트륨 추적을 영구히 쓰려면 Code.gs 를 고쳐야 합니다.**

## 고치는 방법

시트 헤더 행에 `meal`, `na`, `fib`, `sug` 4개 컬럼을 추가하고, `Code.gs` 의
행↔객체 변환을 헤더 기준으로 일반화하면 앞으로 필드가 늘어도 코드를 안 고쳐도 됩니다.

```javascript
// 헤더 행을 기준으로 객체 <-> 행 변환 (컬럼 순서·개수에 의존하지 않음)
function headers_(sheet) {
  return sheet.getRange(1, 1, 1, sheet.getLastColumn()).getValues()[0];
}
function rowToObj_(hdr, row) {
  var o = {};
  hdr.forEach(function (h, i) { if (h) o[h] = row[i]; });
  return o;
}
function objToRow_(hdr, obj) {
  return hdr.map(function (h) { return h && obj[h] !== undefined ? obj[h] : ""; });
}
```

`add` 는 `objToRow_(headers_(sheet), item)` 로 append 하고, `list` 는
`rowToObj_` 로 되돌리면 됩니다. 기존 행의 새 컬럼은 빈 값이 되고, 클라이언트가
빈 값을 0 과 `간식` 으로 처리하므로 과거 기록도 그대로 열립니다.

## 다음 단계

`Code.gs` 내용을 주시면 위 방식으로 정확한 패치를 만들어 드립니다.

---

## 추가된 meta 키 (2026-09-09)

`setMeta` 로 저장하는 키가 늘었습니다. 값은 전부 JSON 한 덩어리라 시트 컬럼과
무관하지만, `Code.gs` 가 키를 화이트리스트로 검사한다면 아래를 허용해야 합니다.

| 키 | 내용 |
|---|---|
| `goals` | 목표 — `kcal p c f na fib` |
| `presets` | 자주 먹는 음식 |
| `plan` | 시간대별 식단 계획 |
| **`calc`** | 열량 계산 설정 — 활동계수·걸음수·웨이트·유산소·적자·매크로% |
| **`act`** | 날짜별 활동 기록 — `{"2026-09-09": {steps, wh, ch, ci}}` |

`act` 는 날짜가 계속 쌓이므로 1년이면 20KB 정도가 됩니다. meta 한 칸에
넣기 부담스러우면 별도 시트로 빼는 게 좋고, 그 경우 `list` 응답에
`act` 를 함께 실어주면 클라이언트는 지금 코드 그대로 동작합니다.

이 두 키가 저장되지 않아도 앱은 동작합니다 — 기기 안(localStorage)에는
남기 때문에 그 폰/PC 에서는 정상이고, 기기 간 동기화만 안 됩니다.

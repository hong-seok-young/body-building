# 구글 시트 연결하기 (10분)

앱은 기본적으로 **폰 안에만**(localStorage) 기록을 저장합니다. 구글 시트를 연결하면
폰·PC 어디서 열어도 같은 데이터가 보이고, 브라우저 데이터를 지워도 기록이 남습니다.

---

## 1. 구글 시트 만들기

브라우저에서 **[sheets.new](https://sheets.new)** 를 엽니다. 빈 시트가 하나 생깁니다.
이름은 아무거나(예: `바디빌딩 기록`). **탭이나 열은 직접 만들지 않아도 됩니다** —
스크립트가 알아서 만듭니다.

## 2. Apps Script 열기

시트 상단 메뉴에서 **확장 프로그램 → Apps Script**.
새 탭에 `myFunction() {}` 같은 기본 코드가 있는 편집기가 열립니다.

## 3. 코드 붙여넣기

편집기에 있던 내용을 **전부 지우고**, 이 저장소의 **`Code.gs`** 내용을 전부 붙여넣습니다.

그리고 맨 위 이 줄을 바꿉니다:

```javascript
const SECRET = "여기에-본인이-정한-긴-비밀키-넣기";
```

→ 예를 들어

```javascript
const SECRET = "bb-7f3a9c2e8d1b4a6f5e0c3d9b8a7f2e1c";
```

**아무 문자열이나 되지만 길고 추측하기 어려운 값으로** 하세요. 이유는 6번에 있습니다.
비밀키를 만들기 귀찮으면 브라우저 주소창에 아래를 넣고 나온 값을 쓰면 됩니다:

```
javascript:prompt("복사하세요", "bb-" + crypto.randomUUID())
```

💾 저장 (Ctrl+S / ⌘S).

## 4. 한 번 실행해서 시트 만들기

편집기 상단 함수 선택 드롭다운에서 **`setupAndTest`** 를 고르고 **실행** ▶ 을 누릅니다.

- 처음엔 **권한 승인**을 요구합니다 → 검토 → 본인 계정 선택 →
  "이 앱은 Google에서 확인하지 않았습니다" 경고가 나오면
  **고급 → (안전하지 않음) 이동** → 허용.
  (본인이 만든 스크립트를 본인 시트에 쓰는 것이라 정상입니다)
- 실행이 끝나면 시트에 **`food` / `inbody` / `meta`** 탭 3개가 생깁니다.
- 실행 로그에 `추가·조회·삭제 정상: OK` 와 `SECRET ... ✅ 변경됨` 이 찍히면 준비 완료입니다.

## 5. 웹 앱으로 배포

편집기 우측 상단 **배포 → 새 배포**.

| 항목 | 값 |
|---|---|
| 유형 (⚙️ 아이콘) | **웹 앱** |
| 설명 | 아무거나 (예: v1) |
| 다음 사용자로 실행 | **나** |
| 액세스 권한이 있는 사용자 | **모든 사용자** |

**배포** → 나오는 **웹 앱 URL** 을 복사합니다.
`https://script.google.com/macros/s/AKfy...../exec` 형태입니다.

> 코드를 고칠 때마다 **배포 → 배포 관리 → ✏️ 수정 → 버전: 새 버전 → 배포** 를
> 해야 반영됩니다. 저장만으로는 안 바뀝니다.

## 6. 앱에 입력

앱을 열고 우측 상단 **⚙️** →

- **Apps Script 웹앱 URL** : 5번에서 복사한 URL
- **비밀키** : 3번에서 정한 SECRET (똑같이)

**저장 & 연결 테스트** → `✓ 연결 성공!` 이 뜨면 끝입니다.
상단에 `● 동기화됨` 이 초록색으로 표시됩니다.

---

## 보안 — 꼭 읽어주세요

"액세스: 모든 사용자"로 배포하는 이유는, 앱이 브라우저에서 직접 요청을 보내는데
구글 로그인 창을 띄울 수 없기 때문입니다. 그래서:

- **URL 을 아는 사람은 누구나 요청을 보낼 수 있고, 막아주는 건 SECRET 하나뿐입니다.**
- URL 과 SECRET 을 **공개 저장소·채팅·스크린샷에 올리지 마세요.**
  (앱에 입력한 값은 폰 안 localStorage 에만 저장되고 저장소에 올라가지 않습니다)
- 이 저장소의 `Code.gs` 는 템플릿입니다. **SECRET 을 바꾼 버전을 저장소에 커밋하지 마세요.**
- 노출된 것 같으면 SECRET 을 새 값으로 바꿔 재배포하고 앱 설정에도 다시 넣으면 됩니다.

## 안 될 때

| 증상 | 원인 / 해결 |
|---|---|
| `✗ 연결 실패 ... bad-secret` | 앱의 비밀키와 `Code.gs` 의 SECRET 이 다릅니다 |
| `✗ 연결 실패 ... Unexpected token` | URL 이 `/exec` 로 끝나지 않거나, 액세스가 "모든 사용자"가 아닙니다 |
| `✗ 연결 실패 ... Failed to fetch` | 배포를 안 했거나(저장만 함), URL 이 잘못됐습니다 |
| 연결은 되는데 기록이 안 보임 | 배포 후 코드를 고쳤다면 **새 버전으로 재배포** 필요 |
| 끼니·나트륨이 다시 열면 사라짐 | 예전 `Code.gs` 를 쓰고 있습니다. 이 저장소의 최신 버전으로 교체하세요 |
| `● 오프라인 (대기 3)` | 네트워크가 끊긴 동안 쌓인 기록입니다. 연결되면 자동으로 올라갑니다 |

## 시트 구조 (참고)

스크립트가 만드는 탭 3개입니다. **헤더 이름 기준으로 읽고 쓰기 때문에
열 순서를 바꾸거나 열을 추가해도 코드를 고칠 필요가 없습니다.**

- **food** — `id date meal name kcal p c f na fib sug`
- **inbody** — `id date weight smm bfm pbf bmi bmr score note`
- **meta** — `key value` (goals / presets / plan / calc / act 를 JSON 으로 한 줄씩)

시트에서 직접 값을 고쳐도 됩니다. 앱에서 다시 불러오면 반영됩니다.

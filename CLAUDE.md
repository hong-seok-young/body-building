# 작업 규칙

## 대답하기 전에 `LOG.md` 를 먼저 읽는다 — 철칙

사용자가 묻거나 말하면 **대답을 쓰기 전에 `LOG.md` 를 읽는다.** 기억에 기대서
답하면 이미 정한 것을 뒤집거나 이미 들은 것을 다시 묻게 된다. 그러면 사용자는
같은 말을 두 번 해야 하고, 그게 이 도구를 쓸 이유를 없앤다.

- **예외가 없다.** "이건 기억난다" 는 믿을 수 없다 — 세션이 압축됐는지 아닌지를
  내가 알 수 없기 때문이다. 기억이 또렷한 느낌과 실제로 남아 있는 것은 다르다.
- 파일을 통째로 읽거나, 길어지면 최근 며칠 + 관련 낱말로 `grep` 한다.
- 이 파일(`CLAUDE.md`)의 몸·목표·계산식·운동 무게도 같이 본다.
- **읽고도 없으면 그때 묻는다.** 없는 것을 그럴듯하게 지어내지 않는다.
  "기록에 없습니다" 가 틀린 답보다 낫다.

## 정해진 것은 그 자리에서 여기 적는다 — 이 파일이 내 기억이다

대화 기억은 세션이 길어지면 요약으로 압축된다. 그때 구두로 정한 규칙과 숫자는
사라진다. **이 파일은 매 세션 시작할 때 다시 읽히므로 압축과 무관하다.**

- 사용자가 무언가를 정하거나 알려주면 **그 답을 하기 전에 먼저 여기 적는다.**
  나중에 몰아서 적으면 그 "나중" 이 오기 전에 압축된다.
- 같은 것을 두 번 묻지 않는다. 물어야 한다면 먼저 이 파일을 `grep` 한다.
- 지운 적 없는데 기억이 안 나면 **안 적은 것이다.** 적었는지부터 확인한다.

### 대화는 `LOG.md` 에 적는다

우리가 나눈 대화를 시간순으로 적는 파일이다. 앱·시트와 아무 상관이 없다 —
오직 세션이 압축돼도 남기 위한 것이다.

- **사용자가 한 말은 전부 그대로 적는다.** 오타·말줄임도 고치지 않는다.
  줄이거나 다듬으면 나중에 "내가 그렇게 말했나" 를 확인할 수 없다.
- **내가 한 말은 결론과 숫자만 남기고 줄인다.** 설명·표·권유는 뺀다.
  기록의 값어치는 무슨 말을 했나가 아니라 무엇이 정해졌나에 있다.
- 시각은 한국시각 24시간. 한 줄에 하나씩, 날짜별로 묶는다.
- **사적인 내용은 가린다** (위 "활동 이름" 규칙과 같다). 공개 저장소다.
  사용자 말을 그대로 적는 규칙보다 이쪽이 우선이고, 가렸으면 가렸다고 알린다.
- 한 덩어리가 끝날 때마다 적는다. 세션 끝에 몰아서 적으면 그 전에 압축된다.

### 기록에는 한국시각을 같이 적는다

무엇을 언제 먹었고 언제 말했는지가 남아야 한다. 시트에는 시각 칸이 없고
(`SHEETS.food` 은 `date`·`meal` 뿐) 열을 늘리면 백엔드 재배포가 필요하다.
그래서 **`inbox.json` 과 이 파일에 적는다** — 둘 다 깃에 영구히 남고 `grep` 으로 찾힌다.

- 모든 `inbox.json` 항목에 **`kst`** 를 넣는다: `"kst": "2026-09-19 13:40"`
  (한국시각 24시간). 내가 그 항목을 적은 시각이다. `applyPayload` 는 모르는 키를
  무시하므로 앱·시트에는 아무 영향이 없다.
- **먹은 시각이 적은 시각과 다르면 `note` 에 둘 다 적는다** ("12:40 에 먹음").
  아침에 먹은 걸 밤에 말해주는 일이 흔하다.
- `at` 은 **실제 한국시각**을 `Z` 로 적는다. 앞당겨 적지 않는다 — 마지막 항목보다
  뒤이기만 하면 되는 값이지만 시간표로 읽히므로 거짓이면 안 된다.
- 이 파일에 적는 것(운동 무게 등)도 날짜와 시각을 같이 적는다.
- 9/19 이전 항목에는 `kst` 가 없다. 그 시각은 `at` 에 들어 있다 (한국시각을 `Z` 로
  적은 값이라 그대로 읽으면 된다).

### 몸 (2026-09-20 14:27 인바디 기준)

| | |
|---|---|
| 체중 | 97.7 kg |
| 제지방 | 74.7 kg |
| 골격근 | 42.4 kg · 체지방 23.0 kg · 체지방률 23.6% |
| 위상각 | **6.3** — 9/09~9/17 이 5.9~6.0 으로 평평했는데 처음 올라갔다 |
| 인바디 BMR | 1,983 — **측정값이 아니라 `제지방 × 21.6 + 370` 공식값이다** (74.7×21.6+370 = 1,984) |
| 허리 | 100.3 cm · 내장지방 10 · 인바디점수 77 |
| 인바디 제지방 오차 | 약 ±1kg — **한 달치 실제 변화보다 크다** |
| 측정 시각 | **9/20 만 14:27 이고 그 전은 전부 18시대다.** 계열 비교에 이게 걸린다 |
| 9/19 | 재려 했으나 안 쟀다 |

측정 이력 (위상각): 9/09 5.9 · 9/14 6.0 · 9/15 6.0 · 9/17 5.9 · **9/20 6.3**

### 목표와 계산식

- **겨울까지 근육 증량 → 겨울 끝에 지방 감량.** 사용자가 정한 방향이다.
- 단백질 = 제지방 × 2.8 = **209g** (9/20 제지방 74.7 기준. 9/17 기준으로는 208 이었다)
- 적자 500 (`calc.deficit`)
- `sed = bmr × 1.18` · `걸음 = 걸음수 × 체중 × 0.0004` · `웨이트 = 시간 × 체중 × 2.5`
- 나트륨 목표 2,000mg
- 지방 하한 = 체중 × 0.6 (59g), 상한 = 체중 × 0.9

### 적자 500 은 유지한다 — 이유를 여기 적어 다시 안 뒤집는다

2026-09-20 에 내가 "흑자 250 으로 바꾸자" 고 했다가 되돌렸다. 근거가 틀렸다.
같은 판단을 또 흔들지 않도록 이유를 남긴다.

**바꾸자고 한 근거 (틀렸다):** 체단백질이 9/09~9/20 에 14.7→14.6→14.4→14.6→14.7
로 평평하니 근육이 안 붙는다고 읽었다. 폭이 0.3kg 이다. 그런데 나는 **같은
메시지에서** 골격근 측정 폭이 8회에 걸쳐 1.2kg 이라고 써놓고, 그보다 작은
흔들림을 신호로 읽었다. 내가 세운 기준을 내가 어긴 것이다.

**유지해야 하는 근거:**

1. **41일 기록이 리컴프가 되고 있다고 말한다.** 8/10 → 9/20 에 체중은 −0.2kg
   인데 제지방 +0.63 · 체지방 −0.83 이다. 둘이 동시에 일어났다. 유지 근처에서
   먹으면서 이게 나오고 있으면 굳이 흑자로 갈 이유가 없다.
2. **체지방률 23.6% 는 흑자를 주기에 높다.** 체지방이 높을수록 흑자분이 지방
   쪽으로 더 간다(Forbes). 20% 를 넘으면 늘어난 체중 중 제지방은 30~40% 뿐이다.
   흑자 250 을 5개월 주면 +4.9kg 중 지방이 3.2kg 이고 체지방률이 25.6% 가 된다.
3. **먼저 줄이면 같은 흑자가 근육으로 더 간다.** 체지방을 낮춰놓고 증량하는
   쪽이 같은 기간에 더 많은 근육으로 끝난다 — 전에 계산해서 사용자에게 보여준
   결론이 그거였다.

**그러니 계획은 이 순서다: 지금 체지방을 낮춘다 → 낮아지면 증량한다.**
사용자가 처음 제안한 "겨울까지 증량 → 겨울 끝에 감량" 의 반대다. 이건 이미
한 번 논의해서 정한 것이고, 새 데이터가 이걸 뒤집으려면 **측정 노이즈 폭보다
큰 변화가 추세로** 나와야 한다 (골격근 1.2kg · 체지방률 2.6%p 보다 큰 것).

### 모든 줄에 `time`·`src` 가 박힌다 — 지운 것은 `log` 시트에

줄마다 **언제** 들어왔고 **어떻게** 들어왔는지가 시트에 남는다. 화면에는 안 띄운다.

| 칸 | 값 |
|---|---|
| `time` | 넣은 시각, 한국시각 `2026-09-20 22:21:17` |
| `src` | **`앱`** (화면에서 손으로) 또는 **`지시`** (inbox.json) |

- **시각은 `nowKst()` 로만 찍는다.** GitHub Action 은 UTC 로 돌아서 `new Date()` 를
  그대로 쓰면 액션이 넣은 줄만 9시간 어긋난다. 시간표로 읽히는 값이라 안 된다.
- `pushItem(item, src)` 한 곳에서 박는다. 앱의 모든 추가 경로(즐겨찾기·검색·복사·
  프리셋·끼니 옮기기·양 고치기)와 `applyPayload` 가 전부 이 함수로 모인다.
- **지운 줄은 `log` 시트에 남긴다** (`at`·`act`·`type`·`date`·`meal`·`name`·`kcal`·`src`).
  시트에서 줄이 사라지면 흔적이 안 남아서, 남은 줄의 `time`·`src` 만으로는 이력이
  끊긴다. 둘을 합쳐야 전체가 된다.
- 양 고치기·끼니 옮기기는 내부적으로 `delItem` + `pushItem` 이라 **지운 쪽과 넣은
  쪽이 둘 다 남는다.** 따로 처리할 것이 없다.
- 넣기는 `log` 에 안 쓴다. 그 줄 자체가 `time`·`src` 를 들고 있어서 중복이고,
  쓰기가 두 배로 늘면 실패 확률도 두 배가 된다.
- `pushItem` 은 응답에 `src` 가 없으면 콘솔에 경고를 찍는다 — 화면에 안 보이는
  값이라 백엔드가 조용히 버려도 눈에 안 띄기 때문이다. 액션 로그로 잡힌다.

### 시트가 진실이다 — 내 장부가 시트를 따라간다

내가 세는 하루 합계는 `inbox.json` 만 보고 만든 것이라 **앱에서 손으로 넣은 줄이
빠진다.** 9/18 이 560kcal 어긋나 있었다. 이럴 때 고칠 것은 시트가 아니라 내 장부다.

- **기억이 안 난다고 하면 지난 줄을 지우지 않는다.** 추측으로 과거를 흔드는 것이
  기록을 오염시킨 원인이었다. 앱에 들어와 있는 줄은 누군가 일부러 눌러서 들어간
  것이고, 안 먹은 것을 적었을 확률보다 내가 못 들은 것일 확률이 높다.
- 같은 날·같은 끼니·같은 이름이 두 줄인 것만 중복 후보로 본다. 끼니가 다르면
  (9/18 의 3끼·4끼 BSN 처럼) 따로 먹은 것으로 본다.
- 열량이 작은 것(아몬드 5개 35kcal 같은)은 맞히려 들지 않는다. 지난 날짜를 여는
  위험이 그 숫자보다 크다.

**9/18 은 값이 세 번 바뀌었다.** 시트를 볼 때마다 그때의 시트가 맞는 값이다.

| 언제 | 값 | 왜 |
|---|---|---|
| 내가 장부로 셀 때 | 2,446 · 단 166.8 | `inbox.json` 만 봤다 — 틀린 값 |
| 9/20 17:47 감사 | 3,006 · 단 266.8 | 손으로 넣은 BSN 두 줄이 있었다 |
| 9/20 23:0x 감사 | **2,726 · 단 216.8** | 그 중 4끼 2스쿱(280)이 앱에서 지워졌다 |

### 시트를 직접 읽어보려면 — `claude/sheet-audit` 브랜치

`inbox.json` 은 내가 넣은 것만 담는다. **사용자가 앱에서 손으로 넣거나 지운 줄은
거기 흔적이 없다.** 그래서 "내 장부" 와 시트가 조용히 어긋난다 — 실제로 9/18 이
560kcal 어긋나 있었다 (손으로 넣은 BSN 두 줄).

- `claude/sheet-audit` 브랜치의 `.github/dump.mjs` 가 시트를 읽어 지시와 대조한다.
  **읽기만 한다.** 그 브랜치에 아무거나 푸시하면 액션이 돌고 로그에 결과가 찍힌다
  (`.github/workflows/dump.yml` 이 `claude/**` 푸시에 걸려 있다).
- 찍히는 것: 손으로 넣은 줄 · 지시로 넣었는데 시트에 없는 줄 · 중복 · 날짜별 합계.
- **컨테이너에서는 `script.google.com` 에 못 닿는다.** 시트를 보려면 이 액션을 거쳐야 한다.
- 며칠에 한 번은 돌려서 장부와 시트가 어긋나지 않았는지 본다.

### 운동 기록은 앱에 자리가 없다

`sets` 는 "자주 먹는 끼니 조합" 이고 활동은 걸음·웨이트 시간·유산소 시간만 받는다.
**종목·무게·횟수·세트를 넣을 칸이 없다.** 그래서 대화에서 말한 것이 전부 날아갔다
(9/17 등운동, 9/19 인클라인).

- 당분간은 `inbox.json` 항목의 `note` 에 적어 남긴다 — 깃에 영구히 남고 `grep` 으로 찾힌다.
- 앱에 칸을 만드는 것은 **사용자가 하라고 했을 때만** 한다.

### 기록된 무게

| 날짜 | 부위 | 종목 | 세트 |
|---|---|---|---|
| 2026-09-17 (수) | 등 | — | 기록 없음 (웨이트 2h · 5,600보) |
| 2026-09-19 (토) 12:16~13:40 | **가슴** | 인클라인 벤치프레스 | 60×10 · **80×10** · 80×8 · 70×8 · 60×8 (볼륨 3,080kg) |
| 2026-09-20 (일) | **하체** | — | 종목·무게 못 받음 (웨이트 2h·5,000보는 임시값) |

- 9/19 는 **"저번과 무게 같음"** 이다. 저번이 언제인지는 안 적혀 있다.
- 80kg×10 → 추정 1RM 약 107kg (Epley·Brzycki 둘 다 106.7). 체중 대비 1.08배.
- 분할은 부위별로 돈다 (수 등 → 토 가슴 → 일 하체). 확정된 분할표는 아직 없다.


## 고쳤으면 바로 커밋하고 푸시한다 — 철칙

사용자는 **GitHub Pages 로 배포된 페이지를 실시간으로 띄워놓고** 보면서 피드백을 준다.
로컬 파일만 고치면 사용자 화면에는 아무 변화가 없다.

- 작업 한 덩어리가 끝나면 즉시 `git commit` → `git push origin main`.
- 요청이 이어져도 로컬에 쌓아두지 말고 그때그때 올린다.
- 로컬 서버·브라우저 프리뷰로 확인하느라 시간 쓰지 않는다. 검증은 배포본에서 사용자가 한다.
- 커밋 전 확인은 인라인 JS 문법 체크로 충분하다 —
  `<script>` 블록을 뽑아 `node --check` 를 돌린다.

푸시 후 사용자는 새로고침만 하면 된다. `sw.js` 가 `index.html` 은 네트워크 우선으로
받아오기 때문에 서비스워커 캐시가 발목을 잡지 않는다.

`inbox.json` 은 푸시하면 GitHub Actions(`시트 자동 반영`)가 헤드리스 크롬으로 앱을
`?auto=1` 로 한 번 열어 시트에 밀어넣는다 — 사용자가 앱을 열지 않아도 구글 시트가
먼저 최신이 된다. 그러니 푸시한 뒤에는 **액션이 초록인지 확인한다.** 빨간색이면
한 건도 안 들어간 것이다 (백엔드가 안 잡히면 `applyInbox` 가 조용히 멈추도록 되어
있고, 액션은 남은 id 를 세어 그걸 실패로 만든다).

## 사진으로 보낸 음식은 엄격한 기준으로 본다 — 철칙

사진 판독에는 ±15% 쯤 오차가 붙는다. 그 범위 안에서 어느 값을 고를지는 정해져 있다.

- **단백질은 낮은 쪽**(−15%), **탄수·지방·나트륨은 높은 쪽**(+15%), 식이섬유도 낮은 쪽.
- 칼로리는 고른 탄단지로 다시 계산한다 — `탄×4 + 단×4 + 지×9`.

가장 그럴듯한 값이 아니라 **불리한 모서리**를 일부러 고르는 것이다. 덜 먹은 걸 더
먹은 걸로 적는 편이, 더 먹은 걸 덜 먹은 걸로 적는 것보다 낫다. 단백질을 후하게
쳐주면 목표를 채운 줄 알고 넘어가는데 실제로는 모자라고, 열량을 적게 잡으면
체지방이 조용히 붙는다. 틀릴 거면 체지방은 덜 먹고 근육은 더 챙기는 쪽으로 틀린다.

**포장 표기값·공식 영양성분표가 있으면 그 값을 그대로 쓴다.** 오차범위가 없으니
깎지도 올리지도 않는다. 이 규칙은 사진·눈대중 추정에만 적용한다. 둘이 섞인
끼니는 추정한 항목에만 적용하고, 어느 쪽이 표기값인지 밝힌다.

## inbox.json 의 `at` — 조용히 건너뛰는 함정

적용된 지시의 `at` 이 그대로 `calc.at` / `goals.at` 에 박힌다. 앱은 그보다 **앞선**
`at` 을 가진 지시를 "내가 더 최근에 직접 바꿨다" 로 보고 건너뛴다 — 에러 없이,
알림에 `열량 설정 건너뜀` 한 줄만 남기고. 게다가 그 id 는 이미 적용 완료로
등록돼서 다시 시도되지도 않는다.

- **새 항목의 `at` 은 목록 마지막 항목보다 뒤여야 한다.** 현재 시각으로 적는 게
  아니라, 마지막 값을 보고 그보다 뒤로 적는다.
- 지난 항목들이 한국시각을 `Z` 로 적어놔서 실제 UTC 보다 9시간 앞서 있다.
- 건너뛴 지시를 되살리려면 `at` 만 고치는 걸로는 안 되고 **id 도 새로** 줘야 한다.

## 인바디·식단에 항목을 늘릴 때 — 백엔드 재배포가 필요하다

`Code.gs` 의 `SHEETS` 에 컬럼 이름만 넣으면 `sheetOf` 가 시트에 열을 자동으로
덧붙인다. 다만 그건 **배포된** 코드가 하는 일이다. 앱이 쓰는 주소는 `/exec` 라
버전에 고정돼 있어서, 파일만 고치고 푸시하면 시트는 그대로다 — 새 값은 백엔드가
헤더 기준으로 매핑하다가 **조용히 버린다.**

- `syncInbody` 는 시트 응답에 **키 자체가 없는** 칸만 내가 들고 있던 값으로 채운다.
  그래서 재배포 전에도 그 기기에서는 값이 살아있지만, 시트에는 안 올라간다.
- **순서는 안 지켜도 된다.** `applyPayload` 가 인바디를 넣은 뒤 시트를 *날것으로*
  다시 읽어서 값이 실제로 있는지 보고, 없으면 던진다 — `applyInbox` 가 등록을
  되돌리므로 재배포한 뒤 앱을 한 번 열면 저절로 다시 들어간다. 그때까지 액션은
  빨갛다. 이 확인에 `syncInbody` 를 쓰면 안 된다. 그건 시트가 모르는 칸을 메워
  주기 때문에 버려진 걸 못 보고 통과시킨다.
- 인바디 항목은 `index.html` 의 `IB_M` 한 줄 + `Code.gs` 의 `SHEETS.inbody` 한
  칸이면 타일·그래프·표·폼·CSV 가 전부 따라온다. 한 판에 같이 그릴 것은
  `IB_CHARTS`, 따로 그릴 것은 `IB_SOLO` 에 키만 넣는다.

## 시트를 못 읽었으면 inbox 를 돌리면 안 된다

적용 기록(`calc.inbox`)은 시트에 있다. 그걸 못 읽은 채로 `applyInbox` 가 돌면
모든 지시가 "처음 보는 것" 이 되어 **과거를 처음부터 다시 적용한다** — 지우고
다시 넣기를 되풀이하며 옛 설정까지 되살린다. 실제로 한 번 그렇게 됐다.

- `fullSync()` 는 성공 여부를 돌려준다. 부팅에서 그 값을 `applyInbox(synced)`
  에 넘기고, 실패면 건너뛴다.
- 그러면 액션은 남은 id 를 세어 빨갛게 뜬다 — 그게 맞는 신호다.

## 기록이 끝난 날은 지시로 건드리지 않는다 — 자물쇠

`applyPayload` 는 **오늘과 어제** 에만 쓴다. 그보다 앞선 날짜의 `food`·`del`·`act`
는 버리고 `🔒 기록이 끝난 날이라 막음` 한 줄을 남긴다.

자물쇠가 **"지금" 을 기준으로 잠기는 것** 이 핵심이다. 지시를 쓸 때 그 날짜는
오늘이라 열려 있지만, 며칠 뒤 같은 지시가 되감기면 이미 잠겨 있다. 그래서 같은
`inbox.json` 을 몇 번을 다시 돌려도 지난 기록은 움직이지 않는다 — 적용 기록이
날아가든, 시트를 못 읽든, 기기가 셋이든 상관없다.

실제로 9/09~9/11 에 먹지도 않은 줄이 다섯 개 들어갔고 열흘 뒤에야 눈에 띄었다.
앞의 두 규칙(시트 못 읽으면 건너뛰기 · 맨 앞에서 다시 읽기)은 *원인* 을 막는
것이고, 이건 원인이 무엇이든 *결과* 를 막는다. 셋 다 있어야 한다.

- 지난 날을 정말 고쳐야 하면 지시에 `"unlock": ["2026-09-10"]` 또는 `"unlock": true`
  를 적는다. 일부러 적는 것이라 되감겨도 내 뜻이다.
- 다만 `unlock` 이 붙은 지시는 `inbox.json` 에 남아 있는 한 언제든 다시 돌 수 있다.
  다 쓴 뒤에는 그 항목을 `inbox.json` 에서 지워서 은퇴시킨다 (목록에 없는 id 는
  다시 적용될 길이 없다).
- **인바디는 잠그지 않는다.** 하루 한 줄이고 비어 있는 칸만 채우는 구조라 몇 번을
  다시 적용해도 결과가 같다. 반대로 잠그면 지난 결과지를 나중에 읽어 넣는 길이
  막힌다 (위상각을 28건 되채운 적이 있다).
- 앱에서 손으로 고치는 것은 막지 않는다. 지난 날을 탭해서 양을 고치는 건 늘 하는
  일이고, 그건 되감기가 아니라 내 손이다.

## 기록 끝 버튼 — 내가 직접 잠그는 자물쇠

식단 탭 큰 숫자 옆의 **기록 끝** 버튼이다 (예전에 누계가 있던 자리). 누르면 그 날은
닫힌다. 닫힌 날은 **지시가 아예 못 건드린다 — `unlock` 을 적어도 안 열린다.**
내 손으로 닫은 것은 내 손으로만 연다.

- 위의 자물쇠가 *시간* 으로 막는 것이라면 이건 *내가* 잠그는 것이다. 그래서 오늘
  저녁을 다 먹고 누르면 그 뒤로는 오늘조차 안 바뀐다.
- 앱에서 손으로 고칠 때도 한 번 막아선다 — "다시 열고 고칠까요?" 에 예 하면
  그 자리에서 열리고 고쳐진다. 닫는 건 안 묻고, 여는 것만 묻는다.
- 저장 자리는 `calc.done` (날짜 배열). `calc` 는 이미 `META_KEYS` 에 있어서
  **백엔드 재배포가 필요 없다.** 400일이 넘으면 앞에서 자르는데, 잘려서 잃는 건
  화면 표시뿐이다 — 그만큼 지난 날은 '오늘·어제' 자물쇠가 이미 잠그고 있다.

## 활동 이름은 화면에 뜬다 — 사적인 것은 `기타` 로 적는다

`act` 의 `xl` 은 식단 탭 목표 줄에 그대로 찍힌다 (`기타 (+178)`). 걸음·웨이트·
유산소로 안 잡히는 소모를 `xk` 에 넣을 때, **그 이름을 무엇이 있었는지 알 수
있게 적으면 안 된다.** 화면은 밖에서 보이고, 기록은 남는다.

- `xl` 은 **`기타`** 로 적는다. 무엇이었는지는 어디에도 안 적는다.
- 커밋 메시지·`note` 도 같다. 깃 기록은 공개 저장소에 영구히 남는다.
- 숫자(`xk`)만 정확하면 계산은 전부 맞는다 — 이름은 계산에 안 쓰인다.

이걸 한 번 어겼다 (9/18). 세션이 넘어가면서 구두로 정한 것이 날아갔기 때문에
여기 적는다 — **대화에서 정한 규칙은 여기 안 적으면 다음 세션에 없는 것이다.**

## 아침 체중은 인바디와 따로 둔다

인바디 탭 맨 위의 **아침 체중** 칸이다. 숫자 하나 치고 버튼 하나면 끝난다 —
인바디 폼은 칸이 열다섯 개라 매일 쓸 물건이 못 된다.

**인바디의 `weight` 에 섞으면 안 된다.** 지금까지 인바디는 전부 저녁 18시대
측정이고 아침 공복 체중과는 1kg 넘게 차이가 난다. 한 계열에 섞으면 7일 평균이
"그날 언제 쟀나" 에 따라 출렁여서 정작 보려던 추세를 덮는다.

- 저장 자리는 `calc.mw` (날짜 → kg). `calc` 는 이미 `META_KEYS` 에 있어
  **백엔드 재배포가 필요 없다.** 400일치만 남긴다.
- 기준점은 오늘이 아니라 **마지막으로 잰 날**이다. 며칠 걸러도 창이 안 비고,
  빠진 날은 평균에서 그냥 뺀다.
- 7일 평균끼리 비교해 주간 변화를 내고, 지방 1kg = 7,700kcal 로 **체중이 말하는
  하루 수지**를 환산한다. 그걸 장부(섭취 − `dayTarget().tdee`)와 견주면 유지
  열량이 얼마나 틀렸는지 나온다. 이게 이 칸을 만든 이유다 — 지금 유지 열량은
  인바디가 제지방에 공식을 곱한 값(`제지방 × 21.6 + 370`)에 활동계수 셋을 더
  얹은 것이라, 통째로 남의 평균치다.
- **아침 체중을 안 적으면 인바디 체중으로 간다** (`wSeries()`). 인바디도 전부
  18시대 측정이라 자기들끼리는 비교가 되지만, 그날 먹은 양에 흔들려 점 하나의
  오차가 아침 공복보다 크다. 그래서 그때만 창을 7일이 아니라 **14일**로 넓혀
  점을 더 모으고, 화면에 "오차 ±200kcal" 을 같이 적는다.
  둘을 **섞지는 않는다** — 아침값과 저녁값이 한 평균에 들어가면 그 평균은
  "그날 언제 쟀나" 를 재는 값이 된다. 아침 체중이 6일치 넘게 쌓이면 그쪽으로 넘어간다.
- 인바디가 없으면 `dayTarget()` 에 `tdee` 가 없어서 검증이 안 나온다. 정상이다.
- **기록 끝 자물쇠를 걸지 않는다.** 되감기로 망가질 수 있는 건 지시가 건드리는
  식단이고, 이건 내가 손으로 숫자 하나 넣는 자리다.

## 적용 기록은 개수로 자르면 안 된다

`markInboxDone` 이 예전에 `.slice(-300)` 으로 오래된 id 부터 버렸다. 목록에서
밀려난 id 는 다음에 "처음 보는 지시" 가 되어 과거를 다시 적용한다 — 그때 이미
정정으로 지웠던 줄까지 되살아난다.

지금은 **`inbox.json` 에 아직 있는 id 만 남긴다.** 목록에 없는 id 는 다시 나올
길이 자체가 없으니 버려도 안전하고, 기록은 `inbox.json` 크기를 절대 넘지 않는다.
5,000건이라는 마지막 한도가 남아 있지만 거기 닿으면 `inbox.json` 을 줄여야 한다는
뜻이라 콘솔에 경고를 찍는다.

## 비우는 것은 채울 것이 손에 있고 나서다

`syncRange` 가 `list` 응답을 받자마자 93일치 캐시를 `[]` 로 비우고 그 다음에
`r.items.forEach` 로 채웠다. 그런데 백엔드가 `ok:true` 만 주고 `items` 를 빼고
오는 일이 실제로 있었다 — 그 한 번에 `forEach` 가 터지면서 **캐시는 비었는데
채워지지 않은 상태**로 남았다. 액션 로그에 `시트에서 읽은 날짜 93일치` 인데
`2026-09-18 — 0줄` 로 찍힌 게 그것이다.

- `api()` 가 `action: "list"` 응답에 `items` 배열이 없으면 거기서 막는다.
  읽기라서 다시 물어봐도 안전하므로 재시도 대상으로 던진다.
- `syncRange` 는 확인한 뒤에 비운다. `syncInbody` 도 `(r.items || [])` 를 걷어냈다 —
  그건 응답이 깨졌을 때 인바디를 통째로 빈 배열로 저장해버리는 길이었다.
- **`|| []` 로 받아주는 것이 항상 친절한 게 아니다.** 없어도 되는 값에는 맞지만,
  "있어야 하는데 없는" 값에 붙이면 사고를 정상처럼 통과시킨다.

이걸 찾은 건 `auto-sync.mjs` 가 `lastSyncErr` 를 찍게 한 덕이다. 그 전까지는
`[inbox] 시트를 못 읽어서 건너뜀` 한 줄뿐이라 네 판을 네트워크 탓으로 봤다.
**실패 이유를 안 찍으면 재실행만 되풀이하게 된다.**

## POST 가 GET 으로 바뀌어 도착한다 — `{ok:true, alive:true}`

`/exec` 는 POST 를 받으면 302 로 넘기고 브라우저가 그걸 GET 으로 따라간다.
그게 가끔 `/exec` 로 되돌아와서 `doGet` 이 대신 답한다:

    {"ok": true, "alive": true, "note": "POST 로 요청하세요"}

`ok:true` 라 그냥 두면 앱은 성공으로 본다. `list` 면 `items` 가 없어서 나중에
엉뚱한 자리에서 터지고(위 항목), **`add` 면 "성공" 으로 보고 `pending` 에서
빼버린다 — 시트에는 한 줄도 안 들어갔는데 기록만 사라진다.**

- `api()` 가 `alive:true` 를 보면 던진다. 읽기만 다시 보낸다.
- 쓰기는 다시 안 보낸다. 리다이렉트가 어디서 꺾였는지 알 수 없어서 이미
  들어갔을 수도 있고, 그러면 두 줄이 된다. 대기열에 남으니 다음 동기화가
  시트를 다시 읽고 나서 처리한다 — 잃는 것보다 늦는 것이 낫다.
- `add`·`del` 응답에 `item`·`id` 가 없으면 같은 이유로 성공으로 보지 않는다.

## 구글이 404 를 뱉으면 읽기만 다시 보낸다

`/exec` 가 가끔 404 를 준다. 스크립트가 돌기도 전에 구글 프런트엔드에서 나는
것이라 잠시 뒤 다시 하면 들어간다 — 9/18 하루에만 네 번 났고 매번 재실행
한 번으로 해결됐다. 문제는 그 한 번이 `fullSync` 를 실패로 끝내고, 그러면
`applyInbox` 가 통째로 멈춰서(`시트를 못 읽어서 건너뜀`) 하루치 기록이 안
들어간다는 것이다.

그래서 `api()` 가 **읽기만** 0.8초·2.4초 간격으로 두 번 더 보낸다
(`READ_ACTIONS` = list · foodSearch · barcode · dbSearch · dbCount).

- **쓰기는 절대 다시 보내지 않는다.** 404 가 스크립트 앞에서 났는지 뒤에서
  났는지 응답만 보고는 알 수 없다. `add` 를 다시 보내면 같은 음식이 두 줄
  들어갈 수 있고, 그게 이 앱을 오염시킨 바로 그 사고다. 읽기는 몇 번을 해도
  결과가 같으니 거기만 손댄다.
- **404·5xx 와 연결 실패만** 다시 시도한다. 로그인 페이지·승인 필요·`ok:false`
  는 다시 보내도 똑같으므로 바로 던진다 — 재시도로 덮으면 진짜 고장이
  "느린 것" 처럼 보인다.
- 세 번이 한계다. 그래도 안 되면 액션이 빨갛게 뜨는 게 맞다.

## 시트를 다시 읽는 자리는 "지우기보다 앞" 이다

`applyPayload` 는 지시가 건드릴 날짜를 시트에서 다시 읽고 시작한다. 캐시는 부팅
때의 스냅샷이라, 폰·PC·액션이 같은 지시를 비슷한 시각에 집으면 셋 다 "아직
없네" 로 보고 각자 넣는다 (약과가 세 줄 들어갔다).

- **읽는 자리가 중요하다.** 지우기 뒤에서 읽으면 방금 낸 삭제가 아직 시트에 안
  닿아서(`delItem` 은 응답을 안 기다린다) 지운 줄이 그대로 돌아오고, 정정 지시의
  "다시 넣기" 를 중복으로 보고 건너뛴다. 한 번 그렇게 깨뜨렸다.
- 그래서 **맨 앞에서** 한 번 읽고, 그 뒤로는 `delItem`·`pushItem` 이 캐시를
  갱신하는 것을 믿는다.

## 구성

| 파일 | 내용 |
|---|---|
| `index.html` | 앱 전체 — 단일 파일 PWA (HTML+CSS+JS) |
| `Code.gs` | Apps Script 백엔드 — 구글 시트 저장, 식약처 DB·바코드 검색 |
| `inbox.json` | 커밋해두면 앱이 열릴 때 자동 반영되는 대기 항목 (`id` 로 중복 방지) |
| `sw.js` / `manifest.json` | 서비스워커 · PWA 매니페스트 |
| `.github/workflows/auto-sync.yml` · `.github/auto-sync.mjs` | `inbox.json` 을 푸시하면 헤드리스 크롬으로 앱을 열어 시트에 바로 반영 |
| `SETUP.md` / `BACKEND.md` | 시트 연결 가이드 · 백엔드 필드 메모 |
| `LOG.md` | 대화 기록 — 앱과 무관하고, 세션 압축을 넘기기 위한 파일 |

화면은 하단 탭 5개다 — **식단**(일간 기록) · **리포트**(주간/월간) · **열량** · **음식**(음식 추가·내 음식·성분표) · **인바디**.

사진 판독(Gemini)은 쓰지 않는다. 제품 등록은 바코드(Open Food Facts)와 식약처 DB 검색으로만 한다.
